// import WebSocket from "ws";

// const wss = new WebSocket.Server({ port: 3005 });

// wss.on("connection", (ws) => {
//     ws.on("somestring", (event) => {
//         console.log(event);
//     });
// });

const delta: number[] = [];
